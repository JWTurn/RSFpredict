After knitting and rendering `RSFpredict.Rmd`, overwrite this file with a symlink to `RSFpredict.md`.

E.g., on Linux/macOS from terminal:

```bash
cd /Users/jwt/git-local/RSFpredict
rm README.md && ln -s RSFpredict.md README.md
```
